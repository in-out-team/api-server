debug: true

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace # set bindings for SQL

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show-sql: true
