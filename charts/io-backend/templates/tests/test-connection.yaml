apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "in-out-api.fullname" . }}-test-connection"
  labels:
    {{- include "in-out-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "in-out-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
